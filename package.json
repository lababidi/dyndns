{
  "name": "dyndns",
  "version": "1.0.0",
  "private": true,
  "description": "dyndns2-compatible updater endpoint that writes to Cloudflare DNS",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
